# Baseline Profile for Square
# Pre-compiles critical code ahead of time (AOT) to eliminate JIT pauses,
# reduce cold-start latency to < 400ms, and maintain 60-120 FPS during initial gestures.

# 1. Whole application: UI, playback, data, backend, native JNI, auth, and application startup
HSPLdev/lelonio/square/**->**(**)**
Ldev/lelonio/square/**;

# 2. Jetpack Compose: runtime, UI layout, draw phase, and foundations
HSPLandroidx/compose/runtime/**->**(**)**
Landroidx/compose/runtime/**;
HSPLandroidx/compose/ui/**->**(**)**
Landroidx/compose/ui/**;
HSPLandroidx/compose/foundation/**->**(**)**
Landroidx/compose/foundation/**;
HSPLandroidx/compose/animation/**->**(**)**
Landroidx/compose/animation/**;
HSPLandroidx/compose/material3/**->**(**)**
Landroidx/compose/material3/**;

# 3. AndroidX Activity, Lifecycle, and Navigation
HSPLandroidx/activity/**->**(**)**
Landroidx/activity/**;
HSPLandroidx/lifecycle/**->**(**)**
Landroidx/lifecycle/**;
HSPLandroidx/navigation/**->**(**)**
Landroidx/navigation/**;

# 4. Asynchronous engine: Kotlin Coroutines & Flow
HSPLkotlinx/coroutines/**->**(**)**
Lkotlinx/coroutines/**;

# 5. Coil Image Loading Pipeline (fast cover art decode and cache hit)
HSPLcoil/**->**(**)**
Lcoil/**;
